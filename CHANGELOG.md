# Changelog

## 0.1.0 (2026-09-06)

### Features

* **core:** pure geometry for move, resize, rotate, viewport matrices, snapping and bounds
* **react:** `Surface` and `useFrame` with pointer and keyboard transactions
* **demo:** dark canvas with zoom, pan, CSS zoom on the parent, grid snapping and a live frame inspector
